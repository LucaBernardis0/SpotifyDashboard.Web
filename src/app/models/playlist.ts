export interface Playlist{
    id: string;
    name: string; // name of the playlist
    image: string; // link to the playlist cover image
    owner: string; // name of the one who created the playlist
    spotifyUrl: string // external link to the spotify page of the playlist
}